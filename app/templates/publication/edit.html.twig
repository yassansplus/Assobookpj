{% extends 'profile/account.html.twig' %}

{% block title %}Modifier la Publication{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('index') }}
    {{ encore_entry_link_tags('publication') }}
{% endblock %}

{% block body %}
    <div class="publication">
        <h2 class="titleMesPubli">Modifier la publication</h2>
        <hr>

        <div>
            <div class="form-group">{{ include('publication/_form.html.twig', {'button_label': 'Mettre à jours'}) }}</div>
            {{ include('publication/_delete_form.html.twig') }}
        </div>
        <a class="btn btn-outline-primary" href="{{ path('publication_index') }}">< Retour aux publications</a>

    </div>
{% endblock %}
